version: "3.8"

services:
  react-app:
    image: ${CI_REGISTRY_IMAGE:-simpeg-frontend}:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=local
    restart: unless-stopped
